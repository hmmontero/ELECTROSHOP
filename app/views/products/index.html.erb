<h1>Lists available products</h1>
<p>what are you looking for?</p>

<div class="container">
  <div class="row">
    <div class="column">
      <ol><% @products.each do |product| %>
        <%= product.name %>
        <li><%= simple_form_for [product, @cart_product] do |f| %></li>
          <%= f.submit "Add to cart" %>
          <%= link_to "See details of #{product.name}", product_path(product) %>
        <% end %>
      <% end %></ol>
    </div>
  </div>
</div>
